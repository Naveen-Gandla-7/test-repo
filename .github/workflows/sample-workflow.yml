name: Test workflow
on:
  push:
    branches:
      - feature
jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Calling REST API to create a repo
        run: |
          curl --location --request POST 'https://api.github.com/user/repos' \
          --header 'Accept: application/vnd.github+json' \
          --header 'Authorization: Bearer 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' \
            --data-raw '{
                "name":"test-repo-10",
                "auto_init": true,
                "description":"created this repo through rest call",
                "gitignore_template":"Android"
            }'
   
   
