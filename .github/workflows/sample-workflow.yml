name: Test workflow
on:
  push:
    branches:
      - feature
jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Calling REST API to create a repo
        run: |
          curl --request POST \
          --url https://api.github.com/user/repos \
          --header 'Authorization: Bearer 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' \
          --header 'Content-Type: application/json' \
          --data-raw '{
          "name":"test-repo-4",
          "auto_init": true,
          "description":"created this repo through rest call",
          "gitignore_template":"Android"
          }' \
