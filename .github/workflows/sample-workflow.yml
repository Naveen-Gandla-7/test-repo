name: Test workflow
on:
  push:
    branches:
      - feature
jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: create a repo
        run: |
          curl --location --request POST 'https://api.github.com/user/repos' \ --header 'Authorization: Bearer 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' \ --header 'Content-Type: application/json' \ --data-raw '{"name":"test-repo-3","auto_init": true,"description":"created this repo through rest call","gitignore_template":"Android"}'
